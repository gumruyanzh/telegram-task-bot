python-telegram-bot[job-queue]>=20.0,<21.0  # Telegram bot library with JobQueue support
pytz>=2023.3  # Timezone handling for PST/PDT conversion
# sqlite3 is built-in with Python, no need to install
